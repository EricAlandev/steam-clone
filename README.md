Para rodar o projeto:

1️⃣ Faça o clone do repositório:
git clone <url-do-repositorio>

2️⃣ Com o repositório já clonado, configure as credenciais do Firebase no arquivo de ambiente.

3️⃣ Em seguida, execute:
npx next dev --webpack
